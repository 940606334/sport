$(document).ready(function () {
   /* $('#msg').bind('input onpropertychange', function() {
        var msg=$(this).val();
        var dialog = YDUI.dialog;
        dialog.toast(msg, 3000);
    });*/
});